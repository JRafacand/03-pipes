import { PipersonalizadoPipe } from './pipersonalizado.pipe';

describe('PipersonalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipersonalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
